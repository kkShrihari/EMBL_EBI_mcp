{
  "name": "EMBL_EBI_mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "MCP server exposing EMBL-EBI Search and Summary (S4) REST APIs",
  "main": "dist/server.js",
  "bin": {
    "ebi-search-mcp-server": "dist/server.js"
  },
  "files": [
    "dist",
    "README.md",
    "CHANGELOG.md"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "start": "node dist/server.js",
    "clean": "rimraf dist",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.9.3"
  }
}
